# GitHub setup

```bash
cd beamio-platform
git init
git add .
git commit -m "Initial Beamio platform import"
git branch -M main
git remote add origin https://github.com/YOUR-ORG/beamio-platform.git
git push -u origin main
```

After pushing, connect the repo in Lovable and use the frontend folder as the app workspace.
